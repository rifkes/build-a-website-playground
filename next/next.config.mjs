/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
	images: {
		domains: ['cdn.sanity.io', 'https://cdn.sanity.io/images/rdd8qwbf/production/',],
	},
};

export default nextConfig;
